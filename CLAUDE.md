# Claude Code 引き継ぎメモ

最終更新: 2026-08-15

## 現在地

- ブランチ: `feat/gym-app-mvp`
- 実装計画: `docs/superpowers/plans/2026-08-14-gym-app.md`
- Task 1〜13 が完了済み（計画書のチェックボックス自体は未更新のため、コミット履歴と実装を正とする）
- 直近完了: Task 13「メンバー一覧と他メンバーの履歴」
- 次に着手するタスク: Task 14「設定画面」（計画書の `### Task 14` から再開）

## Task 13 で追加したもの

- `fetchProfiles()`（表示名昇順、エラー伝播、nullデータは空配列）
- `/members` のメンバー一覧画面
- `/members/:userId` のメンバー別ワークアウト履歴画面
- 一覧・詳細の永続的なエラー表示と再試行導線
- 一覧から詳細へ表示名をrouter stateで渡し、記録0件でも名前を表示
- クエリ・一覧・詳細のテスト9件

## 検証結果

- `npm run build`: 成功
- `npx vitest run --maxWorkers=1 --reporter=dot`: 23 files / 143 tests 全件成功
- 通常の並列 `npm test` は、この環境では今回の変更と無関係な既存テスト2件が5秒でタイムアウトした。ワーカー1つでは全件成功したため、負荷起因と判断。

## 再開時の注意

- UI文言は日本語、コード識別子とコミットメッセージは英語。
- 通信エラーを空状態と同じ表示にしない。画面内にエラーを残し、再試行を用意する既存パターンに合わせる。
- 主要操作のタップ領域は最低56px。
- 作業開始前に `git status --short` を確認する。
